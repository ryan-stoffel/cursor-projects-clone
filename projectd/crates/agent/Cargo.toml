[package]
name = "projectd-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Agent role: worktrees, worker loop, context sync."

[dependencies]
projectd-protocol = { path = "../protocol" }
